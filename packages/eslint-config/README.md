# @erpgsoft/eslint-config

Configuração compartilhada do ESLint para projetos TypeScript e React.

Inclui recomendações do ESLint, regras do `@typescript-eslint` e integração com Prettier.

Uso:

```json
{
  "extends": "@erpgsoft/eslint-config"
}
```
